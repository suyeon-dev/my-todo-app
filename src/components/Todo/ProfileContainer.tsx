import React from 'react';
import { ProfileContainerStyle } from '../../styles/MainPage.styles';

export default function ProfileContainer() {
  return (
    <ProfileContainerStyle>
      <div>{/* (Todo) 프로그레스 바 넣기 */}</div>
    </ProfileContainerStyle>
  );
}
